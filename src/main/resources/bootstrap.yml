spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mnrasul/configurations
          searchPaths: server-config
        bootstrap: true

